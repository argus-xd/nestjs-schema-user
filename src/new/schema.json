{
    "$schema": "http://json-schema.org/schema",
    "$id": "NewProjectByTemplate",
    "title": "Hello Options Schema",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "description": "The name of the module.",
            "$default": {
                "$source": "argv",
                "index": 0
            },
            "x-prompt": "Введите имя автора пакетов"
        },
    },
    "required": [
    ]
}