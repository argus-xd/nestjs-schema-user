{
    "$schema": "http://json-schema.org/schema",
    "$id": "app",
    "title": "Hello Options Schema",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "description": "the app template.",
            "$default": {
                "$source": "argv",
                "index": 0
            },
            "x-prompt": "Enter the name of the package author"
        },
    },
    "required": [
    ]
}